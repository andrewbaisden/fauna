DATABASE_URL="postgresql://fauna:fauna@localhost:5432/fauna?schema=public"
BETTER_AUTH_SECRET="replace-with-a-32-character-or-longer-secret"
BETTER_AUTH_URL="http://localhost:3000"
GITHUB_CLIENT_ID=""
GITHUB_CLIENT_SECRET=""
SENTRY_DSN=""
NEXT_PUBLIC_SENTRY_DSN=""
NEXT_PUBLIC_POSTHOG_KEY=""
NEXT_PUBLIC_POSTHOG_HOST="https://eu.i.posthog.com"
BLOB_READ_WRITE_TOKEN=""
# After creating a Vercel Blob store: pnpm assets:mirror-media && pnpm db:seed
# Neon: use the pooled URL as DATABASE_URL in the Next.js app. For `prisma migrate`, set DIRECT_URL to the unpooled connection (or run migrate against the unpooled URL).
DIRECT_URL=""
